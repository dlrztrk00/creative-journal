# Copy to .env.local. Every value is optional — the site builds and runs
# without any of them.

# Canonical origin, used for metadata, canonical URLs and OG images.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
